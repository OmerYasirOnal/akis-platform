# Definition of Done (DoD)
- Builds and starts: Fastify `/health` returns {status:"ok"}.
- TS strict, ESLint + Prettier pass.
- `.env.example` includes DATABASE_URL and MCP tokens.
- Minimal tests or smoke tests exist.
- README documents local run.
- No secrets committed; `.gitignore` covers env/build/logs.
